# Step 2 — Menu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Рабочий экран `/menu` со списком товаров, 3 категориями (Кофе/Десерты/Не кофе), пустым состоянием для пустых категорий и добавлением в корзину прямо с карточки. Карточки соответствуют Figma (node `85-113`).

**Architecture:** Каталог — статический JSON в `src/data/menu.json`. Корзина — zustand-store с persist в localStorage, минимальный API (`items`, `add`, `totalCount`). Селект категории — локальный state в `MenuPage`. Отдельного экрана карточки товара нет — добавление в корзину происходит «+» кнопкой на карточке списка (упрощение MVP, расходится со спекой).

**Tech Stack:** zustand 4 (новая зависимость), без изменений в остальном.

**Scope reduction vs spec:** Удалена `ProductPage` (`/menu/:id`) — карточка товара. Add-to-cart происходит сразу с карточки списка с количеством 1. Аддоны/опции напитков (молоко/сиропы) тоже выкинуты — не критично для демо.

---

## File Map

**Создаём:**
- `src/data/menuTypes.ts` — типы `Category`, `MenuItem`
- `src/data/menu.json` — каталог из 10 позиций
- `src/data/menuQueries.ts` — `getByCategory(cat)`
- `src/data/menuQueries.test.ts`
- `src/stores/cartStore.ts` — zustand + persist, минимальный API
- `src/stores/cartStore.test.ts`
- `src/components/CategoryTabs/CategoryTabs.tsx` + `.module.css` + `.test.tsx`
- `src/components/MenuItemCard/MenuItemCard.tsx` + `.module.css` + `.test.tsx`
- `src/components/EmptyState/EmptyState.tsx` + `.module.css`
- `src/pages/MenuPage/MenuPage.module.css`

**Модифицируем:**
- `package.json` — добавить `zustand`
- `src/pages/MenuPage/MenuPage.tsx` — реализация
- `src/App.tsx` — убрать роут `/menu/:id` и импорт `ProductPage`
- `src/App.test.tsx` — убрать тест для `/menu/:id`
- `src/styles/theme.css` — добавить `--button-dark: #593a3a`

**Удаляем:**
- `src/pages/ProductPage/` — целиком (не нужен в MVP)

---

## Pre-task: Установка zustand

Не разбивается на TDD-шаги. Просто установка.

- [ ] **Step 0.1: Установить zustand**

```bash
pnpm add zustand
```

Expected: `pnpm-lock.yaml` обновлён, в `dependencies` появилась `zustand`.

---

## Task 1 — Cart store (минимальный API + persist)

**Files:**
- Create: `src/stores/cartStore.ts`, `src/stores/cartStore.test.ts`

- [ ] **Step 1.1: Написать падающий тест**

Файл `/Users/aishelist/Work/miniApp/src/stores/cartStore.test.ts`:

```typescript
import { beforeEach, describe, expect, it } from 'vitest';
import { useCartStore } from './cartStore';

describe('cartStore', () => {
  beforeEach(() => {
    useCartStore.setState({ items: [] });
    localStorage.clear();
  });

  it('starts empty', () => {
    expect(useCartStore.getState().items).toEqual([]);
    expect(useCartStore.getState().totalCount()).toBe(0);
  });

  it('adds a new item with quantity 1', () => {
    useCartStore.getState().add('latte-1');
    const { items, totalCount } = useCartStore.getState();
    expect(items).toEqual([{ menuItemId: 'latte-1', quantity: 1 }]);
    expect(totalCount()).toBe(1);
  });

  it('increments quantity when adding the same item again', () => {
    useCartStore.getState().add('latte-1');
    useCartStore.getState().add('latte-1');
    const { items, totalCount } = useCartStore.getState();
    expect(items).toEqual([{ menuItemId: 'latte-1', quantity: 2 }]);
    expect(totalCount()).toBe(2);
  });

  it('keeps two different items separately', () => {
    useCartStore.getState().add('latte-1');
    useCartStore.getState().add('cappuccino-1');
    useCartStore.getState().add('latte-1');
    const { items, totalCount } = useCartStore.getState();
    expect(items).toHaveLength(2);
    expect(items.find(i => i.menuItemId === 'latte-1')?.quantity).toBe(2);
    expect(items.find(i => i.menuItemId === 'cappuccino-1')?.quantity).toBe(1);
    expect(totalCount()).toBe(3);
  });
});
```

- [ ] **Step 1.2: Запустить — должно упасть**

```bash
pnpm exec vitest run src/stores/
```

Expected: FAIL — `Cannot find module './cartStore'`.

- [ ] **Step 1.3: Реализовать `cartStore.ts`**

Файл `/Users/aishelist/Work/miniApp/src/stores/cartStore.ts`:

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

export type CartItem = {
  menuItemId: string;
  quantity: number;
};

type CartState = {
  items: CartItem[];
  add: (menuItemId: string) => void;
  totalCount: () => number;
};

export const useCartStore = create<CartState>()(
  persist(
    (set, get) => ({
      items: [],
      add: (menuItemId) =>
        set((state) => {
          const existing = state.items.find((i) => i.menuItemId === menuItemId);
          if (existing) {
            return {
              items: state.items.map((i) =>
                i.menuItemId === menuItemId
                  ? { ...i, quantity: i.quantity + 1 }
                  : i
              ),
            };
          }
          return { items: [...state.items, { menuItemId, quantity: 1 }] };
        }),
      totalCount: () => get().items.reduce((sum, i) => sum + i.quantity, 0),
    }),
    { name: 'yc-cart' }
  )
);
```

- [ ] **Step 1.4: Запустить — должно пройти**

```bash
pnpm exec vitest run src/stores/
```

Expected: 4 passed.

---

## Task 2 — Menu types и каталог

**Files:**
- Create: `src/data/menuTypes.ts`, `src/data/menu.json`

- [ ] **Step 2.1: Типы**

Файл `/Users/aishelist/Work/miniApp/src/data/menuTypes.ts`:

```typescript
export type Category = 'coffee' | 'dessert' | 'not-coffee';

export type MenuItem = {
  id: string;
  category: Category;
  name: string;
  description: string;
  price: number;       // в рублях
  bonusPrice: number;  // в бонусах
  image: string;       // URL
};
```

- [ ] **Step 2.2: Каталог из 10 позиций**

Файл `/Users/aishelist/Work/miniApp/src/data/menu.json`:

```json
[
  {
    "id": "espresso",
    "category": "coffee",
    "name": "Эспрессо",
    "description": "Крепкий концентрированный кофе из свежеобжаренных зёрен",
    "price": 150,
    "bonusPrice": 120,
    "image": "https://placehold.co/200x200/593a3a/ffffff?text=%D0%AD%D1%81%D0%BF%D1%80%D0%B5%D1%81%D1%81%D0%BE"
  },
  {
    "id": "cappuccino",
    "category": "coffee",
    "name": "Капучино",
    "description": "Классика на основе эспрессо и взбитого молока",
    "price": 250,
    "bonusPrice": 200,
    "image": "https://placehold.co/200x200/593a3a/ffffff?text=%D0%9A%D0%B0%D0%BF%D1%83%D1%87%D0%B8%D0%BD%D0%BE"
  },
  {
    "id": "latte",
    "category": "coffee",
    "name": "Латте",
    "description": "Эспрессо с большим количеством молока и нежной пенкой",
    "price": 280,
    "bonusPrice": 230,
    "image": "https://placehold.co/200x200/593a3a/ffffff?text=%D0%9B%D0%B0%D1%82%D1%82%D0%B5"
  },
  {
    "id": "raf-lavender",
    "category": "coffee",
    "name": "Раф с лавандой",
    "description": "Авторский сладкий кофе с нежной пенкой и лавандой",
    "price": 350,
    "bonusPrice": 300,
    "image": "https://placehold.co/200x200/593a3a/ffffff?text=%D0%A0%D0%B0%D1%84"
  },
  {
    "id": "cheesecake",
    "category": "dessert",
    "name": "Чизкейк Нью-Йорк",
    "description": "Классический американский десерт со сливочным сыром",
    "price": 320,
    "bonusPrice": 280,
    "image": "https://placehold.co/200x200/8f6341/ffffff?text=%D0%A7%D0%B8%D0%B7%D0%BA%D0%B5%D0%B9%D0%BA"
  },
  {
    "id": "brownie",
    "category": "dessert",
    "name": "Брауни",
    "description": "Шоколадный десерт с орехами и насыщенным вкусом",
    "price": 220,
    "bonusPrice": 180,
    "image": "https://placehold.co/200x200/8f6341/ffffff?text=%D0%91%D1%80%D0%B0%D1%83%D0%BD%D0%B8"
  },
  {
    "id": "cheese-dessert",
    "category": "dessert",
    "name": "Сырный десерт",
    "description": "Воздушный десерт с кремовой текстурой",
    "price": 250,
    "bonusPrice": 200,
    "image": "https://placehold.co/200x200/8f6341/ffffff?text=%D0%A1%D1%8B%D1%80%D0%BD%D1%8B%D0%B9"
  },
  {
    "id": "matcha-latte",
    "category": "not-coffee",
    "name": "Матча латте",
    "description": "Японский зелёный чай маття с молоком",
    "price": 320,
    "bonusPrice": 270,
    "image": "https://placehold.co/200x200/ff9149/ffffff?text=%D0%9C%D0%B0%D1%82%D1%87%D0%B0"
  },
  {
    "id": "cocoa",
    "category": "not-coffee",
    "name": "Какао",
    "description": "Горячий шоколадный напиток с маршмеллоу",
    "price": 200,
    "bonusPrice": 160,
    "image": "https://placehold.co/200x200/ff9149/ffffff?text=%D0%9A%D0%B0%D0%BA%D0%B0%D0%BE"
  },
  {
    "id": "sea-buckthorn-tea",
    "category": "not-coffee",
    "name": "Чай облепиховый",
    "description": "Согревающий чай с ягодами облепихи и мёдом",
    "price": 180,
    "bonusPrice": 150,
    "image": "https://placehold.co/200x200/ff9149/ffffff?text=%D0%9E%D0%B1%D0%BB%D0%B5%D0%BF%D0%B8%D1%85%D0%B0"
  }
]
```

Заметки: используем `placehold.co` с цветами из брендовой палитры (коричневый/охра/оранжевый) и названием напитка. Когда захочешь — замени на реальные фото (например, Unsplash), просто меняешь URL в этом JSON.

---

## Task 3 — Menu queries

**Files:**
- Create: `src/data/menuQueries.ts`, `src/data/menuQueries.test.ts`

- [ ] **Step 3.1: Падающий тест**

Файл `/Users/aishelist/Work/miniApp/src/data/menuQueries.test.ts`:

```typescript
import { describe, expect, it } from 'vitest';
import { getByCategory } from './menuQueries';

describe('getByCategory', () => {
  it('returns coffee items for category=coffee', () => {
    const items = getByCategory('coffee');
    expect(items.length).toBeGreaterThan(0);
    expect(items.every((i) => i.category === 'coffee')).toBe(true);
    expect(items.find((i) => i.id === 'raf-lavender')).toBeDefined();
  });

  it('returns dessert items for category=dessert', () => {
    const items = getByCategory('dessert');
    expect(items.length).toBeGreaterThan(0);
    expect(items.every((i) => i.category === 'dessert')).toBe(true);
  });

  it('returns non-coffee items for category=not-coffee', () => {
    const items = getByCategory('not-coffee');
    expect(items.length).toBeGreaterThan(0);
    expect(items.every((i) => i.category === 'not-coffee')).toBe(true);
  });
});
```

- [ ] **Step 3.2: Запустить — должно упасть**

```bash
pnpm exec vitest run src/data/
```

Expected: FAIL.

- [ ] **Step 3.3: Реализовать**

Файл `/Users/aishelist/Work/miniApp/src/data/menuQueries.ts`:

```typescript
import menuData from './menu.json';
import type { Category, MenuItem } from './menuTypes';

const allItems = menuData as MenuItem[];

export function getByCategory(category: Category): MenuItem[] {
  return allItems.filter((item) => item.category === category);
}
```

- [ ] **Step 3.4: Запустить — должно пройти**

```bash
pnpm exec vitest run src/data/
```

Expected: 3 passed.

---

## Task 4 — Расширить палитру

**Files:**
- Modify: `src/styles/theme.css`

- [ ] **Step 4.1: Добавить тёмно-коричневый**

В файле `/Users/aishelist/Work/miniApp/src/styles/theme.css` найти строку:

```css
  --ai-card: #8f6341;       /* (если такого комментария нет — найти эту строку без комментария) */
```

(в текущем файле она без комментария: `--ai-card: #8f6341;`). После неё добавить:

```css
  --button-dark: #593a3a;
```

Итоговый блок переменных должен выглядеть так (показываю только новую строку в контексте):

```css
  --ai-card: #8f6341;
  --button-dark: #593a3a;
  --text: #000000;
```

---

## Task 5 — CategoryTabs component

**Files:**
- Create: `src/components/CategoryTabs/CategoryTabs.tsx`, `.module.css`, `.test.tsx`

- [ ] **Step 5.1: Падающий тест**

Файл `/Users/aishelist/Work/miniApp/src/components/CategoryTabs/CategoryTabs.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { CategoryTabs } from './CategoryTabs';

describe('CategoryTabs', () => {
  it('renders three tabs', () => {
    render(<CategoryTabs value="coffee" onChange={() => {}} />);
    expect(screen.getByRole('tab', { name: /Кофе/ })).toBeInTheDocument();
    expect(screen.getByRole('tab', { name: /Десерты/ })).toBeInTheDocument();
    expect(screen.getByRole('tab', { name: /Не кофе/ })).toBeInTheDocument();
  });

  it('marks the active tab as selected', () => {
    render(<CategoryTabs value="dessert" onChange={() => {}} />);
    expect(screen.getByRole('tab', { name: /Десерты/ })).toHaveAttribute('aria-selected', 'true');
    expect(screen.getByRole('tab', { name: /Кофе/ })).toHaveAttribute('aria-selected', 'false');
  });

  it('calls onChange when a tab is clicked', async () => {
    const user = userEvent.setup();
    const onChange = vi.fn();
    render(<CategoryTabs value="coffee" onChange={onChange} />);
    await user.click(screen.getByRole('tab', { name: /Десерты/ }));
    expect(onChange).toHaveBeenCalledWith('dessert');
  });
});
```

Note: `vi.fn()` и `userEvent` доступны в vitest+RTL без явных импортов через `globals: true`. Если упадёт с unknown identifier — добавь импорты:
```tsx
import { vi } from 'vitest';
import userEvent from '@testing-library/user-event';
```

- [ ] **Step 5.2: Установить @testing-library/user-event если ещё нет**

Проверь `package.json`. Если нет — установи:
```bash
pnpm add -D @testing-library/user-event
```

- [ ] **Step 5.3: Запустить — должно упасть**

```bash
pnpm exec vitest run src/components/CategoryTabs/
```

Expected: FAIL.

- [ ] **Step 5.4: Реализовать `CategoryTabs.tsx`**

Файл `/Users/aishelist/Work/miniApp/src/components/CategoryTabs/CategoryTabs.tsx`:

```tsx
import { Coffee, IceCream, GlassWater } from 'lucide-react';
import type { Category } from '../../data/menuTypes';
import styles from './CategoryTabs.module.css';

type Props = {
  value: Category;
  onChange: (next: Category) => void;
};

const tabs: { id: Category; label: string; Icon: typeof Coffee }[] = [
  { id: 'coffee', label: 'Кофе', Icon: Coffee },
  { id: 'dessert', label: 'Десерты', Icon: IceCream },
  { id: 'not-coffee', label: 'Не кофе', Icon: GlassWater },
];

export function CategoryTabs({ value, onChange }: Props) {
  return (
    <div role="tablist" className={styles.tabs}>
      {tabs.map(({ id, label, Icon }) => {
        const selected = value === id;
        return (
          <button
            key={id}
            type="button"
            role="tab"
            aria-selected={selected}
            className={`${styles.tab} ${selected ? styles.active : ''}`.trim()}
            onClick={() => onChange(id)}
          >
            <Icon size={16} aria-hidden="true" />
            <span>{label}</span>
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 5.5: Стили `CategoryTabs.module.css`**

Файл `/Users/aishelist/Work/miniApp/src/components/CategoryTabs/CategoryTabs.module.css`:

```css
.tabs {
  display: flex;
  gap: 8px;
  margin-bottom: 16px;
}

.tab {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 14px;
  border-radius: 15px;
  border: 1px solid rgba(0, 0, 0, 0.1);
  background: var(--surface);
  color: rgba(0, 0, 0, 0.5);
  font-size: 14px;
  line-height: 1;
  cursor: pointer;
  transition: background 120ms ease, color 120ms ease;
}

.tab:hover {
  border-color: rgba(0, 0, 0, 0.25);
}

.active {
  background: var(--button-dark);
  color: var(--surface);
  border-color: rgba(0, 0, 0, 0.25);
}

.active:hover {
  border-color: rgba(0, 0, 0, 0.25);
}
```

- [ ] **Step 5.6: Запустить — должно пройти**

```bash
pnpm exec vitest run src/components/CategoryTabs/
```

Expected: 3 passed.

---

## Task 6 — MenuItemCard component

**Files:**
- Create: `src/components/MenuItemCard/MenuItemCard.tsx`, `.module.css`, `.test.tsx`

- [ ] **Step 6.1: Падающий тест**

Файл `/Users/aishelist/Work/miniApp/src/components/MenuItemCard/MenuItemCard.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { MenuItemCard } from './MenuItemCard';
import type { MenuItem } from '../../data/menuTypes';

const item: MenuItem = {
  id: 'raf-lavender',
  category: 'coffee',
  name: 'Раф с лавандой',
  description: 'Авторский сладкий кофе с нежной пенкой и лавандой',
  price: 350,
  bonusPrice: 300,
  image: 'https://example.com/img.png',
};

describe('MenuItemCard', () => {
  it('renders name, description, price and bonus price', () => {
    render(<MenuItemCard item={item} onAdd={() => {}} />);
    expect(screen.getByText('Раф с лавандой')).toBeInTheDocument();
    expect(screen.getByText(/Авторский сладкий/)).toBeInTheDocument();
    expect(screen.getByText('350 ₽')).toBeInTheDocument();
    expect(screen.getByText('или 300 Б')).toBeInTheDocument();
  });

  it('renders product image with alt = item name', () => {
    render(<MenuItemCard item={item} onAdd={() => {}} />);
    const img = screen.getByAltText('Раф с лавандой') as HTMLImageElement;
    expect(img).toBeInTheDocument();
    expect(img.src).toContain('example.com/img.png');
  });

  it('calls onAdd with item id when "+" button is clicked', async () => {
    const user = userEvent.setup();
    const onAdd = vi.fn();
    render(<MenuItemCard item={item} onAdd={onAdd} />);
    await user.click(screen.getByRole('button', { name: /Добавить/ }));
    expect(onAdd).toHaveBeenCalledWith('raf-lavender');
  });
});
```

- [ ] **Step 6.2: Запустить — должно упасть**

```bash
pnpm exec vitest run src/components/MenuItemCard/
```

Expected: FAIL.

- [ ] **Step 6.3: Реализовать `MenuItemCard.tsx`**

Файл `/Users/aishelist/Work/miniApp/src/components/MenuItemCard/MenuItemCard.tsx`:

```tsx
import { Plus } from 'lucide-react';
import type { MenuItem } from '../../data/menuTypes';
import styles from './MenuItemCard.module.css';

type Props = {
  item: MenuItem;
  onAdd: (id: string) => void;
};

export function MenuItemCard({ item, onAdd }: Props) {
  return (
    <article className={styles.card}>
      <img className={styles.image} src={item.image} alt={item.name} />
      <div className={styles.content}>
        <h3 className={styles.name}>{item.name}</h3>
        <p className={styles.description}>{item.description}</p>
        <div className={styles.prices}>
          <span className={styles.price}>{item.price} ₽</span>
          <span className={styles.bonus}>или {item.bonusPrice} Б</span>
        </div>
      </div>
      <button
        type="button"
        className={styles.addButton}
        onClick={() => onAdd(item.id)}
        aria-label={`Добавить ${item.name} в корзину`}
      >
        <Plus size={20} aria-hidden="true" />
      </button>
    </article>
  );
}
```

- [ ] **Step 6.4: Стили `MenuItemCard.module.css`**

Файл `/Users/aishelist/Work/miniApp/src/components/MenuItemCard/MenuItemCard.module.css`:

```css
.card {
  position: relative;
  display: grid;
  grid-template-columns: 102px 1fr;
  gap: 12px;
  padding: 12px;
  border-radius: 10px;
  background: var(--surface);
  border: 1px solid rgba(0, 0, 0, 0.15);
  box-shadow: 0 4px 4px rgba(0, 0, 0, 0.1);
}

.image {
  width: 102px;
  height: 113px;
  object-fit: cover;
  border-radius: 10px;
  display: block;
}

.content {
  display: flex;
  flex-direction: column;
  min-width: 0;
  padding-right: 40px; /* зарезервируем место под "+" */
}

.name {
  font-size: 18px;
  font-weight: 600;
  line-height: 1.2;
  margin-bottom: 4px;
}

.description {
  font-size: 13px;
  line-height: 1.3;
  color: var(--text-muted);
  margin-bottom: auto;
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.prices {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-top: 8px;
}

.price {
  font-size: 15px;
  font-weight: 600;
  color: var(--text);
}

.bonus {
  font-size: 12px;
  color: var(--highlight);
}

.addButton {
  position: absolute;
  right: 12px;
  bottom: 12px;
  width: 31px;
  height: 31px;
  border-radius: 8px;
  background: var(--button-dark);
  color: var(--surface);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(0, 0, 0, 0.1);
  cursor: pointer;
  transition: transform 80ms ease;
}

.addButton:active {
  transform: scale(0.96);
}
```

- [ ] **Step 6.5: Запустить — должно пройти**

```bash
pnpm exec vitest run src/components/MenuItemCard/
```

Expected: 3 passed.

---

## Task 7 — EmptyState component

**Files:**
- Create: `src/components/EmptyState/EmptyState.tsx`, `.module.css`

Без отдельного теста — компонент чисто презентационный, без логики.

- [ ] **Step 7.1: Компонент**

Файл `/Users/aishelist/Work/miniApp/src/components/EmptyState/EmptyState.tsx`:

```tsx
import { Hourglass } from 'lucide-react';
import styles from './EmptyState.module.css';

type Props = {
  title: string;
  description: string;
};

export function EmptyState({ title, description }: Props) {
  return (
    <div className={styles.wrapper}>
      <h2 className={styles.title}>{title}</h2>
      <p className={styles.description}>{description}</p>
      <Hourglass size={80} strokeWidth={1.5} className={styles.icon} aria-hidden="true" />
    </div>
  );
}
```

- [ ] **Step 7.2: Стили**

Файл `/Users/aishelist/Work/miniApp/src/components/EmptyState/EmptyState.module.css`:

```css
.wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  padding: 40px 16px 0;
}

.title {
  font-size: 28px;
  font-weight: 700;
  margin-bottom: 12px;
}

.description {
  font-size: 15px;
  color: rgba(0, 0, 0, 0.69);
  margin-bottom: 80px;
  max-width: 320px;
}

.icon {
  color: var(--text);
  opacity: 0.6;
}
```

---

## Task 8 — Собрать MenuPage

**Files:**
- Modify: `src/pages/MenuPage/MenuPage.tsx`
- Create: `src/pages/MenuPage/MenuPage.module.css`, `src/pages/MenuPage/MenuPage.test.tsx`

- [ ] **Step 8.1: Падающий тест**

Файл `/Users/aishelist/Work/miniApp/src/pages/MenuPage/MenuPage.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { MemoryRouter } from 'react-router-dom';
import { MenuPage } from './MenuPage';
import { useCartStore } from '../../stores/cartStore';

describe('MenuPage', () => {
  beforeEach(() => {
    useCartStore.setState({ items: [] });
    localStorage.clear();
  });

  it('renders title "Меню"', () => {
    render(<MemoryRouter><MenuPage /></MemoryRouter>);
    expect(screen.getByRole('heading', { name: 'Меню' })).toBeInTheDocument();
  });

  it('renders coffee items by default', () => {
    render(<MemoryRouter><MenuPage /></MemoryRouter>);
    expect(screen.getByText('Раф с лавандой')).toBeInTheDocument();
    expect(screen.getByText('Капучино')).toBeInTheDocument();
  });

  it('switches to desserts when tab clicked', async () => {
    const user = userEvent.setup();
    render(<MemoryRouter><MenuPage /></MemoryRouter>);
    await user.click(screen.getByRole('tab', { name: /Десерты/ }));
    expect(screen.getByText('Брауни')).toBeInTheDocument();
    expect(screen.queryByText('Раф с лавандой')).not.toBeInTheDocument();
  });

  it('adds item to cart when "+" is clicked', async () => {
    const user = userEvent.setup();
    render(<MemoryRouter><MenuPage /></MemoryRouter>);
    await user.click(screen.getByRole('button', { name: /Добавить Капучино/ }));
    expect(useCartStore.getState().totalCount()).toBe(1);
    expect(useCartStore.getState().items[0].menuItemId).toBe('cappuccino');
  });
});
```

- [ ] **Step 8.2: Запустить — должно упасть**

```bash
pnpm exec vitest run src/pages/MenuPage/
```

Expected: 4 failed (старый компонент рендерит просто `<h2>Меню</h2>`, ожидания не сойдутся, плюс ловить будет CategoryTabs и MenuItemCard, которых ещё нет в MenuPage).

- [ ] **Step 8.3: Реализовать `MenuPage.tsx`**

Заменить содержимое `/Users/aishelist/Work/miniApp/src/pages/MenuPage/MenuPage.tsx`:

```tsx
import { useState } from 'react';
import { CategoryTabs } from '../../components/CategoryTabs/CategoryTabs';
import { MenuItemCard } from '../../components/MenuItemCard/MenuItemCard';
import { EmptyState } from '../../components/EmptyState/EmptyState';
import { getByCategory } from '../../data/menuQueries';
import { useCartStore } from '../../stores/cartStore';
import type { Category } from '../../data/menuTypes';
import styles from './MenuPage.module.css';

const emptyMessages: Record<Category, { title: string; description: string }> = {
  coffee: {
    title: 'Скоро в меню',
    description: 'Мы готовим новые виды кофе. Совсем скоро они появятся в нашей кофейне',
  },
  dessert: {
    title: 'Скоро в меню',
    description: 'Мы готовим новые десерты. Совсем скоро они появятся в нашей кофейне',
  },
  'not-coffee': {
    title: 'Скоро в меню',
    description: 'Мы готовим новые напитки. Совсем скоро они появятся в нашей кофейне',
  },
};

export function MenuPage() {
  const [category, setCategory] = useState<Category>('coffee');
  const items = getByCategory(category);
  const add = useCartStore((s) => s.add);

  return (
    <div className={styles.page}>
      <h1 className={styles.title}>Меню</h1>
      <CategoryTabs value={category} onChange={setCategory} />
      {items.length === 0 ? (
        <EmptyState
          title={emptyMessages[category].title}
          description={emptyMessages[category].description}
        />
      ) : (
        <div className={styles.list}>
          {items.map((item) => (
            <MenuItemCard key={item.id} item={item} onAdd={add} />
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 8.4: Стили `MenuPage.module.css`**

Файл `/Users/aishelist/Work/miniApp/src/pages/MenuPage/MenuPage.module.css`:

```css
.page {
  padding-top: 4px;
}

.title {
  font-size: 32px;
  font-weight: 700;
  margin-bottom: 16px;
}

.list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
```

- [ ] **Step 8.5: Запустить — должно пройти**

```bash
pnpm exec vitest run src/pages/MenuPage/
```

Expected: 4 passed.

---

## Task 9 — Удалить ProductPage и роут

**Files:**
- Delete: `src/pages/ProductPage/` (целиком)
- Modify: `src/App.tsx`, `src/App.test.tsx`

- [ ] **Step 9.1: Удалить файлы ProductPage**

```bash
rm -rf /Users/aishelist/Work/miniApp/src/pages/ProductPage
```

- [ ] **Step 9.2: Убрать роут и импорт из `App.tsx`**

В `/Users/aishelist/Work/miniApp/src/App.tsx`:

Удалить строку:
```tsx
import { ProductPage } from './pages/ProductPage/ProductPage';
```

Удалить строку:
```tsx
          <Route path="/menu/:id" element={<ProductPage />} />
```

Файл целиком после правки:

```tsx
import { Routes, Route } from 'react-router-dom';
import { BottomNav } from './components/BottomNav/BottomNav';
import { HomePage } from './pages/HomePage/HomePage';
import { MenuPage } from './pages/MenuPage/MenuPage';
import { CartPage } from './pages/CartPage/CartPage';
import { ProfilePage } from './pages/ProfilePage/ProfilePage';
import styles from './App.module.css';

export function App() {
  return (
    <div className={styles.app}>
      <main className={styles.main}>
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/menu" element={<MenuPage />} />
          <Route path="/cart" element={<CartPage />} />
          <Route path="/profile" element={<ProfilePage />} />
        </Routes>
      </main>
      <BottomNav />
    </div>
  );
}
```

- [ ] **Step 9.3: Убрать тест `/menu/:id` из `App.test.tsx`**

Удалить блок:

```tsx
  it('renders ProductPage with id at /menu/:id', () => {
    render(
      <MemoryRouter initialEntries={['/menu/latte-1']}>
        <App />
      </MemoryRouter>
    );
    expect(screen.getByRole('heading', { name: 'Товар latte-1' })).toBeInTheDocument();
  });
```

Также тест `it('renders MenuPage at /menu', ...)` теперь нужно подправить — потому что MenuPage больше не рендерит `<h2>Меню</h2>` (рендерит `<h1>` через MenuPage.tsx). Заменить:

```tsx
  it('renders MenuPage at /menu', () => {
    render(
      <MemoryRouter initialEntries={['/menu']}>
        <App />
      </MemoryRouter>
    );
    expect(screen.getByRole('heading', { name: 'Меню' })).toBeInTheDocument();
  });
```

(Тут `getByRole('heading')` без уровня сматчит `<h1>` — уже работает корректно.)

Итоговое содержимое `/Users/aishelist/Work/miniApp/src/App.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { App } from './App';

describe('App routing', () => {
  it('renders HomePage at / with greeting', () => {
    render(
      <MemoryRouter initialEntries={['/']}>
        <App />
      </MemoryRouter>
    );
    expect(screen.getByRole('heading')).toHaveTextContent(/Главная.*Алексей/);
  });

  it('renders MenuPage at /menu', () => {
    render(
      <MemoryRouter initialEntries={['/menu']}>
        <App />
      </MemoryRouter>
    );
    expect(screen.getByRole('heading', { name: 'Меню' })).toBeInTheDocument();
  });
});
```

⚠️ Уточнение: на странице `/menu` теперь рендерятся ещё карточки товаров, у которых тоже есть заголовки `<h3>`. `getByRole('heading', { name: 'Меню' })` сматчит именно один с этим именем — конфликта нет.

- [ ] **Step 9.4: Прогнать все тесты — всё зелёное**

```bash
pnpm test
```

Expected: 26 passed (новые из Step 2 — cart 4 + queries 3 + CategoryTabs 3 + MenuItemCard 3 + MenuPage 4 = 17; из Step 1 — App: 2 (после удаления `/menu/:id`-теста), BottomNav: 3, Telegram: 4 = 9). Итого 17 + 9 = 26.

Если выйдет другое число — посчитай и сверь руками; иногда reading bundle suite может пропустить новый файл, тогда проверь, что vitest подобрал твои тесты.

---

## Task 10 — Прогнать билд и проверить визуально

- [ ] **Step 10.1: Сборка**

```bash
pnpm run build
```

Expected: успешно, без TS-ошибок.

- [ ] **Step 10.2: Запустить dev-сервер**

```bash
pnpm run dev
```

Открой `http://localhost:5173/menu` в браузере. Проверь:

1. Заголовок «Меню».
2. Три таба, по умолчанию активен «Кофе» (тёмно-коричневый).
3. Список из 4 кофейных позиций. Каждая карточка: фото-плейсхолдер слева, название/описание справа, цена + бонусная цена жёлтым, тёмная «+» кнопка справа-снизу.
4. Клик на «+» — карточка слегка прожимается, ничего не падает. Открой DevTools → Application → Local Storage → `yc-cart` — должна появиться запись с `items`.
5. Клик на таб «Десерты» — список меняется на десерты.
6. Клик на таб «Не кофе» — список меняется на 3 напитка.
7. Перезагрузка страницы (F5) — корзина сохраняется в localStorage.

Останови dev-сервер (Ctrl+C).

---

## Definition of Done для Шага 2

- ✅ `pnpm test` — 21 passed (или больше, если добавились дополнительные).
- ✅ `pnpm run build` — без ошибок.
- ✅ В браузере на `/menu` видны 3 таба + список товаров + плейсхолдеры с фото.
- ✅ Клик «+» добавляет в корзину; перезагрузка сохраняет состояние.
- ✅ Пустой таб (если случится) показывает EmptyState с песочными часами.
- ✅ ProductPage удалён, нет битых импортов.
- ✅ Все изменения в working tree без коммитов — пользователь коммитит сам.

После этого можно идти на **Шаг 3 — Корзина и оформление заказа** (отдельный план).
